@page
@using Microsoft.AspNetCore.Mvc.Localization
@using Acme.OnlineCourses.Localization
@inject IHtmlLocalizer<OnlineCoursesResource> L
@model Acme.OnlineCourses.Pages.IndexModel

@section styles {
    <abp-style src="/Pages/Index.cshtml.css" />
}
@section scripts {
    <abp-script src="/Pages/Index.cshtml.js" />
}

<div class="row mb-3">
    <div class="col-12">
        <div class="card">
            <div class="card-body">
                <h3 class="card-title">@Model.FeaturedBlog?.Title</h3>
                <div class="text-muted mb-3">
                    @if (Model.FeaturedBlog?.PublishedDate != null)
                    {
                        <small>@Model.FeaturedBlog.PublishedDate.Value.ToString("dd/MM/yyyy")</small>
                    }
                </div>
                <div class="blog-content">
                    @Html.Raw(Model.FeaturedBlog?.Content)
                </div>
            </div>
        </div>
    </div>
</div>
